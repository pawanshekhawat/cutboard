# CutBoard Studio runtime config

# Backend API base URL
VITE_CUTBOARD_API_BASE=http://localhost:3001

# Optional explicit project path on the server machine.
# If omitted, backend default project path is used.
# VITE_CUTBOARD_PROJECT_PATH=./project.json
